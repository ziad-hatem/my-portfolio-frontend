import type { Config } from "tailwindcss";

const config: Config = {
  // Tailwind v4 uses CSS-based configuration via @theme in globals.css
  // Most animations are built-in to v4
  plugins: [],
};

export default config;
